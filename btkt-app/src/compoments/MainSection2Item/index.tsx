import React from 'react'
import { images } from '../../data/images'
import Images from '../Images'

const MainSection2Item = ({children}:{children : React.ReactNode}) => {
    return (
      <li>
        {children}
      </li>
    );
}

export default MainSection2Item